<!DOCTYPE html>
<html>
  <head>
    <title>Library Books</title>
    <link rel="stylesheet" href="/style.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  </head>
  <body>
    <h1 class="bg-success text-center p-3">📚 Library Books</h1>

    <nav class="bg-warning d-flex justify-content-center gap-4 p-3">
      <a href="/addBook">➕ Add New Book</a>
      <a href="/books">📖 Show All Books</a>
      <a href="/home">🏠 Home</a>
    </nav>

    <ul class="mt-4">
      <% if (books.length === 0) { %>
        <p class="text-center">No books found.</p>
      <% } else { %>
        <% books.forEach(book => { %>
          <li>
            <strong><%= book.title %></strong> - <%= book.author %>
            (<%= book.mrp %>, <%= book.year %>)
            <a href="/books/edit/<%= book._id %>">✏️ Edit</a>
            <a href="/books/delete/<%= book._id %>">🗑️ Delete</a>
          </li>
        <% }) %>
      <% } %>
    </ul>
  </body>
</html>
